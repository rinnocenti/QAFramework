# Audio QA Harness

This QA area validates `com.immersive.audio` as an independent package consumer.

It does not validate gameplay and does not require `FrameworkRuntimeHost`. The framework boot may be skipped when the scene is played in `Current Scene Only` mode.

## Canonical Flow

Use this flow:

```text
1. Immersive Framework QA > Audio > Create or Refresh Audio QA Scene
2. Open Assets/ImmersiveFrameworkQA/Audio/Scenes/QA_Audio.unity
3. Enter Play Mode
4. Run All Audio Smokes
```

The create/refresh command also ensures generated WAV clips and assigns them to positive cues. The separate `Immersive Framework QA > Audio > Repair Generated Audio Clips` menu remains available as a maintenance tool, but it is not a required setup step.

## Generated Files

The builder creates or refreshes:

```text
Assets/ImmersiveFrameworkQA/Audio/Scenes/QA_Audio.unity
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_AudioDefaults.asset
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_SfxCue.asset
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_SfxCue_Pooled.asset
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_SfxCue_MissingClip.asset
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_AudioSfxPool.asset
Assets/ImmersiveFrameworkQA/Audio/ScriptableObjects/QA_BgmCue.asset
Assets/ImmersiveFrameworkQA/Audio/AudioClips/QA_Sfx_Beep.wav
Assets/ImmersiveFrameworkQA/Audio/AudioClips/QA_Bgm_Tone.wav
Assets/ImmersiveFrameworkQA/Audio/Prefabs/QA_PooledAudioSource.prefab
```

The audio clips are synthetic sine tones generated by the scene builder. They are not final game assets.

## Scene Contents

`QA_Audio.unity` contains:

```text
Audio QA Root
  QA_AudioRuntimeHost
  QA_AudioRuntimeHost_MissingDefaults
  QA_AudioRuntimeHost_MissingPool
  QA_PoolRuntimeHost
  QA_AudioListenerRuntimeHost
  QA_AudioPanel
```

The panel uses IMGUI and does not require TextMeshPro or UGUI setup.

## Panel Buttons

- `Compose Runtime Host` - validates settings resolution from `AudioDefaultsAsset`.
- `Play SFX` - plays the direct SFX cue.
- `Play BGM` - plays the BGM cue.
- `Stop BGM` - stops the active BGM.
- `Run Direct SFX Smoke` - validates SFX playback result and handle creation.
- `Run Missing Clip Smoke` - validates explicit `FailedMissingClip`.
- `Run Missing Defaults Smoke` - validates explicit `FailedMissingDefaults` when the host has no defaults.
- `Run Pooled SFX Smoke` - validates explicit pooled SFX with `PoolRuntimeHost`.
- `Run Missing Pool Smoke` - validates explicit `FailedMissingPoolService`.
- `Run Listener Smoke` - validates `AudioListenerRuntimeHost` report-only duplicate detection.
- `Run BGM Smoke` - validates BGM play and stop.
- `Run All Audio Smokes` - runs the full synthetic sequence.
- `Reset QA Counters` - clears panel counters.

## Expected PASS Criteria

With `Run All Audio Smokes`:

```text
[AUDIO_QA] PASS. Direct SFX smoke ...
[AUDIO_QA] PASS. Missing clip smoke ...
[AUDIO_QA] PASS. Missing defaults smoke ...
[AUDIO_QA] PASS. Pooled SFX smoke ...
[AUDIO_QA] PASS. Missing pool smoke ...
[AUDIO_QA] PASS. Listener smoke ...
[AUDIO_QA] PASS. BGM smoke ...
[AUDIO_QA] PASS. All Audio QA smokes completed. passed='7' failed='0'.
```

The exact order may include intermediate PASS lines from individual button operations.

## What This QA Proves

- `AudioDefaultsAsset` serializes and is consumed by `AudioRuntimeHost`.
- SFX direct playback returns `Succeeded` with a valid handle.
- SFX pooled playback returns `Succeeded` with a valid handle and returns rented sources to the pool.
- BGM playback and stop return explicit results.
- Cue missing clip returns `FailedMissingClip`.
- Missing defaults returns `FailedMissingDefaults`; no silent fallback is used.
- Missing pool returns `FailedMissingPoolService`; no direct fallback is used.
- `AudioListenerRuntimeHost` reports duplicates and does not destroy or disable them when policy is `ReportOnly`.
- `com.immersive.audio` can be consumed without `com.immersive.framework`.

## What This QA Does Not Test

- AudioMixer routing. Current routing is metadata resolution only.
- Final game UI or FIRSTGAME usage.
